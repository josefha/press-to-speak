# Copy this file to .env.production for website-distributed builds.
# This file is bundled into the app during `make production-export`.

# Request timeout (seconds)
TRANSCRIPTION_REQUEST_TIMEOUT_SECONDS=45

# Production PressToSpeak API endpoint.
TRANSCRIPTION_PROXY_URL=https://api.your-domain.com/v1/voice-to-text
TRANSCRIPTION_PROXY_API_KEY=

# Supabase client auth (required for PressToSpeak Account mode).
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_your_public_key

# Production export forces this to false.
PRESS_TO_SPEAK_MOCK_ACCOUNT_AUTH=false

# Optional defaults forwarded to API provider parameters.
ELEVENLABS_MODEL_ID=scribe_v1

# Legacy direct-transcription fallback settings (not used in primary flow).
ELEVENLABS_API_KEY=
ELEVENLABS_API_BASE_URL=https://api.elevenlabs.io
